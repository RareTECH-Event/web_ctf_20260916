# このファイルをコピーして .env を作成し、各フラグの値を設定する
# .env は .gitignore 対象（配布イメージにも含めない）
# フラグ値は challenges.md（CTF問題仕様書）に準拠

FLASK_SECRET_KEY=change-me-in-real-deployment

FLAG_01=FLAG{welcome_to_the_ctf_world}
FLAG_02=FLAG{dont_forget_to_clean_up_your_code}
FLAG_03=FLAG{robots_txt_is_not_a_security_measure}
FLAG_04=FLAG{cookies_should_never_be_trusted_blindly}
FLAG_05=FLAG{sql_injection_1s_still_alive_in_2026}
FLAG_06=FLAG{idor_lets_you_peek_into_others_rooms}
FLAG_07=FLAG{path_traversal_climbs_out_of_the_sandbox}
FLAG_08=FLAG{ssti_turns_templates_into_code}
FLAG_09=FLAG{alg_none_means_no_signature_needed}
FLAG_10=FLAG{pickle_deserialization_gives_you_rce}
